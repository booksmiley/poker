# full trainer checkpoints are large (100MB+); only distilled .gto play
# blueprints (see distill.py) belong in git
*.pkl

# training logs and runtime artifacts
train.log
nohup.out

# python
__pycache__/
*.pyc

# editors / os
.idea/
.DS_Store
